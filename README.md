# Run the node scripts without actually knowing it
## Goal:
* Push devs to make the scripts more meaningful

**Build from source:**

1. Ubuntu(Debian):
   run ``cargo deb``
2. Windows
   run ``cargo build --release --target x86_64-pc-windows-gnu``